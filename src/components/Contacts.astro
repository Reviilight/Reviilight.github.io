---
import { contacts } from "@/data/contacts";
import ContactButton from "@/components/ui/ContactButton.astro";

const { itemAmount = Infinity, showSorting = true } = Astro.props;
---

{
    contacts
        .slice(0, itemAmount)
        .map((contact) => <ContactButton contact={contact} />)
}
